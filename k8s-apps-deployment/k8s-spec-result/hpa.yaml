apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  namespace: vote
  name: hpa-result
  labels:
    app: hpa-result
spec:
  maxReplicas: 5
  minReplicas: 2
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: result
  targetCPUUtilizationPercentage: 50
